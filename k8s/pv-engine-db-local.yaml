apiVersion: v1
kind: PersistentVolume
metadata:
  name: engine-db
  labels:
    app: engine-db
spec:
  capacity:
    storage: 2Gi
  accessModes: [ ReadWriteOnce ]
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /srv/opensbpm/volumes/engine-database
